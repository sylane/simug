# Task 7.7: Integration test harness consolidation

## Objective
- Reduce repeated integration-test setup and convert duplicated scenario variants into reusable helpers plus explicit matrices.

## Scope
- In: integration test helper builders, scenario-matrix refactors, changelog/planning/history updates.
- Out: runtime behavior changes, non-test refactors, protocol changes.

## Key Decisions
- Start with the no-open-PR issue-triage integration cluster, where setup duplication and scenario-only differences were highest, instead of attempting a broad rewrite of the entire integration suite in one task.
- Introduce small helpers for shared runner installation and no-open-PR triage fixtures, then collapse the duplicated comment-idempotency cases into one table-driven scenario matrix.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass
- Final: `GOCACHE=/tmp/go-build go test ./internal/app` -> pass
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> total coverage `72.7%`
- Final: `GOCACHE=/tmp/go-build go build -o bin/simug ./cmd/simug` -> pass
- Final: `scripts/canary-real-codex-gate.sh` -> pass (`.simug/canary/real-codex/gate-20260313T130731Z/summary.json`)

## Residual Risks
- The larger integration file still contains other duplicated setup patterns; this task only consolidates the highest-signal no-open-PR triage cluster.

## Planning Refinements
- None.
