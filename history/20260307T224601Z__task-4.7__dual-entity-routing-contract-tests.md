# Task 4.7: Dual-entity output routing contract tests

## Objective
- Add deterministic tests for coordinator vs manager channel routing and unprefixed output quarantine behavior.

## Scope
- In: prompt/parse routing contract (`SIMUG:` vs `SIMUG_MANAGER:`) and quarantine semantics.
- Out: full interactive manager control-plane implementation.

## Key Decisions
- Extend agent parsing to recognize two explicit channels:
  - `SIMUG:` for machine actions,
  - `SIMUG_MANAGER:` for manager-facing human messages.
- Quarantine non-empty unprefixed output lines as structured data instead of silently dropping them.
- Update prompt builders to explicitly instruct manager-prefix usage and quarantine behavior.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> Pass
- Baseline: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> Pass
- Baseline: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> Pass
- Final: `GOCACHE=/tmp/go-build go test ./...` -> Pass
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> Pass
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> Pass

## Residual Risks
- Manager channel is currently logged and audited; a full interactive manager control plane is still future work.

## Planning Refinements
- Updated `Task 4.8` scope to include channel-prefix abuse attempts in prompt-injection resilience fixtures.
