# Task 7.2b: Execution scope lock and repair containment

## Objective
- Bind execution and repair attempts to the approved bootstrap intent so Codex cannot silently switch task scope during retries.

## Scope
- In: execution/repair prompt constraints, post-run scope validation, tests, and docs/changelog updates.
- Out: protocol echo parser hardening (`7.2c`) and post-execution report gate (`7.2d`).

## Key Decisions
- Added a strict execution scope lock derived from approved bootstrap intent (`Task <id>`) and captured `docs/PLANNING.md` baseline.
- Enforced runtime validation that:
  - non-target task statuses in planning cannot change during execution/repair attempts,
  - `[IN_PROGRESS]` cardinality is at most one and must target the locked task when present.
- Extended repair prompts with scope-lock containment instructions so retries are constrained to the approved task.
- Tightened intent validation: `task_ref` must include canonical `Task <id>` so scope lock is machine-derivable.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> total `69.9%`.
- Final: `scripts/canary-real-codex-gate.sh` -> fail (`codex preflight failed: runtime paths appear unwritable` in this environment).

## Residual Risks
- Prompt/template `SIMUG:` echo contamination can still cause false terminal-cardinality failures; tracked by Task `7.2c`.
- Post-execution report payload gate and commit-proof validation are still pending; tracked by Task `7.2d`.
- Real-Codex gate remains blocked in this environment until Codex runtime path permissions are corrected.

## Planning Refinements
- None.
