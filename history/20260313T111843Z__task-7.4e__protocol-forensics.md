# Task 7.4e: protocol forensics

## Objective
- Harden protocol failure forensics so parser/runtime failures always retain the exact active-turn and ignored protocol evidence.

## Scope
- In: bounded-envelope parsing forensics, attempt archive metadata/artifacts, protocol/runtime regressions, changelog/planning sync
- Out: protocol schema changes, gate execution changes, unrelated planning reprioritization

## Key Decisions
- Protocol forensics now classify raw `SIMUG:` lines into accepted active-turn evidence and ignored out-of-envelope evidence so duplicate/stale snippets remain diagnosable after parser failures.
- Attempt archives write explicit placeholders for empty raw output or transcript content so parser/runtime failures never leave blank forensic artifacts.
- Runtime regressions cover echoed out-of-envelope snippets, duplicated terminals, and transcript archive fidelity without introducing protocol-schema changes.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass
- Final: `GOCACHE=/tmp/go-build go test ./internal/agent ./internal/app` -> pass
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> total 73.2%
- Final: `GOCACHE=/tmp/go-build go build -o bin/simug ./cmd/simug` -> pass
- Deferred: `scripts/canary-real-codex-gate.sh` not run in this commit-producing execution turn per orchestration lock; required as follow-up validation.

## Residual Risks
- Live real-Codex evidence for the new forensic metadata remains deferred until the required follow-up gate turn runs.

## Planning Refinements
- None
