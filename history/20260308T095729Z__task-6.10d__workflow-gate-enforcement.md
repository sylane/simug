# Task 6.10d: Workflow enforcement of real-Codex gate for all future tasks

## Objective
- Make real-Codex canary gate evidence mandatory in workflow/agent rules so future tasks cannot be considered done without runtime validation (or explicit manager waiver).

## Scope
- In: `docs/WORKFLOW.md`, `AGENTS.md`, `docs/PLANNING.md` enforcement wording and runbook placement clarity.
- Out: runtime/code changes for Codex integration behavior itself (handled in task `6.10c`).

## Key Decisions
- Require `scripts/canary-real-codex-gate.sh` pass evidence as a Definition-of-Done criterion in workflow and agents instructions.
- Keep explicit manager waiver path documented in planning acceptance wording for exceptional cases.
- Clarify that `docs/REAL_CODEX_GATE.md`, `docs/SANDBOX_DRY_RUN.md`, `docs/SELF_HOST_CANARY.md`, `docs/CHAOS_STOP_RESTART.md`, and `docs/SELF_HOST_GO_NO_GO.md` are operational runbooks, not design/backlog/history replacements.
- Declare history files immutable after commit; clarifications must be added in new history files.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass (latest local run before docs-only change).
- Final: docs/planning consistency review for gate wording and runbook taxonomy -> pass.

## Residual Risks
- Strict gate requirement may slow purely-doc tasks unless manager waiver is used; this is intentional to keep runtime confidence high.

## Planning Refinements
- Added and completed task `6.10d`.
- Refined task `6.10c` to explicitly require passing real-Codex gate with environment-configured Codex and no custom Codex home overrides.
