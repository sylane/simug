# Task 7.1: Enforce orchestrator/project ownership boundary

## Objective
- Remove orchestrator runtime write paths that mutate project workflow/planning/source files.

## Scope
- In: `internal/app` issue-triage/bootstrap flow, runtime write-path audit, design/docs updates tied to ownership boundary.
- Out: broader bootstrap context abstraction and protocol redesign beyond this task.

## Key Decisions
- Removed orchestrator-side planning insertion (`ensureIssueDerivedPlanningTask`) from issue-triage flow; `needs_task=true` now records intent via event log only.
- Kept bootstrap/backlink flows operational without requiring `pending_task_id` by making issue-to-PR backlink markers/body task-id optional.
- Deleted obsolete planning insertion implementation/tests and moved shared `normalizeOneLine` helper into `internal/app/textutil.go` for non-planning usage (`issueUpdateIdempotencyKey`).
- Updated `docs/DESIGN.md` to remove stale transitional divergence wording and document that runtime writes are `.simug/*` only.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out && GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> pass (`total: 68.2%`).
- Final: `go build -o bin/simug ./cmd/simug` -> pass.
- Final: `rg -n "os\\.WriteFile\\(|os\\.OpenFile\\(|os\\.MkdirAll\\(|os\\.Remove\\(" internal --glob '!**/*_test.go'` -> confirms non-test write paths are runtime-artifact paths (`.simug/*` through runtimepaths/archive/eventlog/lock/state).
- Final: `scripts/canary-real-codex-gate.sh` -> fail (preflight: unwritable `~/.codex/tmp/arg0`).
- Final: `CODEX_HOME=/tmp/simug-codex-home CODEX_SQLITE_HOME=/tmp/simug-codex-sqlite scripts/canary-real-codex-gate.sh` -> fail (Codex API auth missing, 401 Unauthorized).

## Residual Risks
- Real-Codex gate remains unverified in this environment due runtime-path permission and authentication failures; task evidence includes failing artifact directories under `.simug/canary/real-codex/`.

## Planning Refinements
- Updated `docs/PLANNING.md` Task 7.2 with a refinement note: define explicit issue-task bootstrap handoff/backlink semantics now that runtime planning insertion and `pending_task_id` assignment are removed from triage.
