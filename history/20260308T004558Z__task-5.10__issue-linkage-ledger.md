# Task 5.10: PR-scoped tracked issue ledger in worker state

## Objective
- Persist issue linkage intents per PR with deterministic idempotency keys so restart/retry preserves linkage context.

## Scope
- In: `.simug/state.json` schema extension, orchestrator intent recording, state persistence/integration tests, design updates.
- Out: posting issue comments and merge-time issue closure.

## Key Decisions
- Added `issue_links` persistence in `.simug/state.json` with per-entry PR number, issue number, relation, comment body, provenance, and deterministic idempotency key.
- Implemented deterministic idempotency keys from canonical `(pr, issue, relation, comment)` payload hashing to keep restart/retry de-duplication stable.
- Recorded `issue_update` intents in orchestrator action handling, while keeping actual issue comment/closure side effects deferred to later tasks.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Focused: `GOCACHE=/tmp/go-build go test ./internal/state ./internal/app` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.

## Residual Risks
- Ledger records are persisted and deduplicated, but orchestrator posting/finalization side effects still depend on tasks 5.11 and 5.12.

## Planning Refinements
- None.
