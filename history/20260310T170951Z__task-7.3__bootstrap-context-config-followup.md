# Task 7.3: Bootstrap context config follow-up

## Objective
- Finish Task 7.3 by making bootstrap guidance and planning discovery configurable per repository while preserving optional-file fallback behavior.

## Scope
- In: bootstrap guidance/planning candidate configuration, prompt/scope-lock regression coverage, and docs/changelog/task-record updates.
- Out: protocol changes, branch-policy changes, and broader orchestration refactors.

## Key Decisions
- Extend discovery with repo-relative environment-configured candidate paths instead of replacing the safe default guidance/planning filenames.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> pass (`total: 70.9%`).
- Final: `GOCACHE=/tmp/go-build go build -o bin/simug ./cmd/simug` -> pass.
- Final: `scripts/canary-real-codex-gate.sh` -> fail before task validation because Codex runtime preflight reports unwritable `~/.codex/tmp/arg0` / `~/.codex` paths in this environment.

## Residual Risks
- Real-Codex gate evidence remains blocked by local Codex runtime permissions, so this follow-up relied on automated Go test/build coverage rather than a live Codex validation run.

## Planning Refinements
- None.
