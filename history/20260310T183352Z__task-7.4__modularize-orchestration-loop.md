# Task 7.4: Modularize orchestration loop

## Objective
- Extract focused orchestration components from `internal/app/run.go` without changing fail-closed behavior.

## Scope
- In: `internal/app` orchestration mode handling, validation/mutation flow, and targeted tests
- Out: behavior changes, protocol/schema changes, and planning changes outside Task 7.4

## Key Decisions
- Extract mode dispatch and fail-closed bootstrap sequencing into `internal/app/orchestration_modes.go` so `run.go` retains startup/prompt/utility responsibilities instead of the full orchestration loop.
- Move GitHub mutation/application helpers and issue finalization/comment formatting into `internal/app/orchestration_mutations.go` so side-effect paths are isolated from mode selection and prompt construction.
- Introduce focused state-transition helpers in `internal/app/orchestration_state.go` and cover them with unit tests to reduce duplicated bootstrap/managed-PR state resets without changing runtime behavior.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> total coverage `72.8%`
- Final: `GOCACHE=/tmp/go-build go build -o bin/simug ./cmd/simug` -> pass
- Final: `scripts/canary-real-codex-gate.sh` -> fail in this environment due unwritable default `~/.codex` runtime paths
- Final: `CODEX_HOME=/tmp/codex-home CODEX_SQLITE_HOME=/tmp/codex-sqlite scripts/canary-real-codex-gate.sh` -> progressed past runtime-path preflight, then failed with Codex API auth `401 Unauthorized`

## Residual Risks
- Real-Codex validation gate is still blocked by environment authentication after working around the local Codex-home permission issue, so only local build/test coverage was revalidated in this run.

## Planning Refinements
- None.
