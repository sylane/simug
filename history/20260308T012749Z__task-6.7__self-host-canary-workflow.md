# Task 6.7: End-to-end self-hosting canary workflow

## Objective
- Provide a repeatable self-hosting canary script that validates simug-on-simug execution continuity across restart boundaries.

## Scope
- In: self-host canary script, docs/changelog/planning/history updates, script contract tests.
- Out: go/no-go policy checklist formalization (Task 6.8).

## Key Decisions
- Implemented self-host canary as a two-phase wrapper around `self-host-loop.sh` to force an explicit restart boundary while preserving existing loop behavior.
- Canary writes a summary artifact referencing both phase logs and canonical runtime artifacts for auditable continuity checks.
- Added dedicated runbook with clear pass/fail and failure-triage steps.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Focused: `GOCACHE=/tmp/go-build go test ./cmd/simug` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Coverage: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Coverage summary: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> 68.9% total statements.

## Residual Risks
- Script validates artifact continuity and restart boundary execution structure; successful dogfood semantics still depend on live manager/orchestrator usage conditions.

## Planning Refinements
- Refined Task 6.8 scope to explicitly require self-host canary and real-Codex gate artifacts in go/no-go criteria.
