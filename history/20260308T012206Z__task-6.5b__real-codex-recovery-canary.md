# Task 6.5b: Real Codex repair/restart interaction canary

## Objective
- Add repeatable real-Codex canaries for repair-path interaction and restart/recovery boundary validation.

## Scope
- In: real-runtime gated recovery canary tests, wrapper script, docs/changelog/planning/history updates.
- Out: release gating policy integration (Task 6.5c).

## Key Decisions
- Added env-gated `internal/app` canary tests so deterministic CI is preserved while operators can execute live runtime checks explicitly.
- Split canary coverage into two scenarios: repair-path interaction (`runAgentWithValidation` retry behavior) and restart boundary recovery action persistence.
- Reused 6.5a artifact root conventions through `SIMUG_CANARY_OUT_DIR` for continuity.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Focused: `GOCACHE=/tmp/go-build go test ./internal/app ./cmd/simug` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Coverage: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Coverage summary: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> 68.9% total statements.

## Residual Risks
- Real runtime canary execution is still environment-dependent and was not executed in this commit without `SIMUG_REAL_CODEX=1` and runtime credentials.

## Planning Refinements
- Refined Task 6.5c to explicitly integrate both protocol and recovery canary runners into release/operator validation gates.
