# Task 5.13: Lifecycle integration + adversarial tests

## Objective
- Expand deterministic test coverage for issue lifecycle and adversarial protocol/state conditions.

## Scope
- In: integration tests for restart persistence, malformed issue linkage payloads, unauthorized issue scope handling, and merge finalization behavior.
- Out: runtime behavior changes outside test/documentation updates.

## Key Decisions
- Added restart-aware lifecycle test using two consecutive `RunOnce` executions with persisted `.simug/state.json` to validate implementation-update -> merge-finalization continuity.
- Added adversarial tests at integration level (not parser-only) to validate coordinator rejection/handling paths under realistic managed/no-open PR loops.
- Kept runtime behavior unchanged for this task; scope is test hardening plus planning refinement.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Focused: `GOCACHE=/tmp/go-build go test ./internal/app -run 'TestRun(NoOpenPRMergeFinalizationSkipsOutOfScopeIssue|ManagedTickRejectsMalformedIssueUpdatePayload|OnceIssueLifecyclePersistsAcrossRestartUntilMergeFinalization)$'` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Coverage: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Coverage summary: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> 68.0% total statements.

## Residual Risks
- End-to-end lifecycle with real GitHub + real Codex remains unvalidated until planned Phase 6 live canary tasks.

## Planning Refinements
- Refined Task 6.5 scope to explicitly include issue-link finalization fault paths (scope rejection, duplicate markers, close failures) in failure-injection coverage.
