# Task 6.5a: Real Codex protocol conformance canary

## Objective
- Provide repeatable canary execution against a real Codex command for protocol conformance with archived artifacts.

## Scope
- In: canary runner script, real-runtime gated test harness, docs/changelog/planning/history updates.
- Out: repair/restart canary scenarios (Task 6.5b).

## Key Decisions
- Implemented the canary as an env-gated Go test (`SIMUG_REAL_CODEX=1`) so normal CI remains deterministic while operators can run real-runtime checks on demand.
- Added a dedicated wrapper script to standardize invocation and artifact root handling for repeatable manual/operator runs.
- Canary scenarios include both expected-success protocol outputs and an expected malformed-protocol failure case to test parser/error-path conformance.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Focused: `GOCACHE=/tmp/go-build go test ./internal/agent ./cmd/simug` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Coverage: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Coverage summary: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> 68.9% total statements.

## Residual Risks
- Real Codex canary execution itself is environment-dependent and was not run in this commit without `SIMUG_REAL_CODEX=1` and runtime credentials.

## Planning Refinements
- Refined Task 6.5b to require repair/restart canary artifact continuity using 6.5a output roots.
