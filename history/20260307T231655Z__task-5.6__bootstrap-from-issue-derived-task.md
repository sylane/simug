# Task 5.6: Bootstrap from issue-derived task

## Objective
- Bootstrap Codex development explicitly from the inserted issue-derived `pending_task_id` while preserving existing bootstrap invariants.

## Scope
- In: bootstrap prompt targeting, pending-task state handling, integration tests for issue-derived kickoff behavior.
- Out: PR backlink posting to issue timeline.

## Key Decisions
- Control-flow decision: when mode is `issue_triage` and `pending_task_id` is non-empty, skip issue re-triage and transition directly to `task_bootstrap`.
- Prompting decision: bootstrap prompt now includes explicit kickoff instruction (`Start specifically with Task <pending_task_id> ...`) so Codex targets the inserted issue-derived task deterministically.
- State decision: preserve `pending_task_id` across idle bootstrap outcomes to support restart/retry targeting until a managed PR is created.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Focused: `GOCACHE=/tmp/go-build go test ./internal/app` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Coverage: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out && GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> pass (`total: 65.5%`).

## Residual Risks
- If `docs/PLANNING.md` is manually changed and removes/renames the stored `pending_task_id`, bootstrap targeting may reference a stale task ID until next triage/insertion cycle refreshes state.

## Planning Refinements
- Refined Task 5.7 scope to explicitly consume `active_issue` + `pending_task_id` context for issue backlink comments.
