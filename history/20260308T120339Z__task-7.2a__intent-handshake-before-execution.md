# Task 7.2a: Intent-only planning handshake before execution

## Objective
- Add a read-only bootstrap intent stage that is validated and persisted before any execution-stage code edits/commits can start.

## Scope
- In: bootstrap mode state/prompt/validation flow, tests, and design/planning/changelog updates.
- Out: repair containment, parser echo hardening, and session continuity tasks (`7.2b+`).

## Key Decisions
- Introduced a staged `task_bootstrap` contract:
  - intent stage (read-only on `main`) requires one `INTENT_JSON` comment and terminal `done` with `changes=false` (or `idle`),
  - execution stage is allowed only after orchestrator persists a validated `bootstrap_intent`.
- Persisted approved bootstrap intent in `.simug/state.json` so restarts preserve intent context before execution.
- Kept issue-triage and pending-task behavior deterministic while adapting integration tests to the staged flow.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> total `68.2%`.
- Final: `scripts/canary-real-codex-gate.sh` -> fail (`codex preflight failed: runtime paths appear unwritable` in this environment).

## Residual Risks
- Prompt/template echo contamination can still create false multi-terminal failures; tracked by Task `7.2c`.
- Repair attempts are not yet scope-locked to approved intent semantics; tracked by Task `7.2b`.
- Real-Codex gate remains blocked in this environment until Codex runtime path permissions are corrected.

## Planning Refinements
- None.
