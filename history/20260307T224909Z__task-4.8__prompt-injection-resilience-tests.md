# Task 4.8: Prompt-injection resilience tests

## Objective
- Add adversarial regression tests that prove instruction-injection attempts are contained by policy.

## Scope
- In: fixtures for malicious GitHub comment content and channel-prefix abuse attempts.
- Out: new runtime authorization policy.

## Key Decisions
- Add adversarial command-parser fixtures that include instruction-injection text, fake `SIMUG:`/`SIMUG_MANAGER:` lines, and embedded `/agent` text not at line start.
- Add parser-prefix abuse fixture for agent output (`SIMUG_MANAGER :` spacing variant) to ensure only exact prefixes are routed and abuse is quarantined.
- Keep policy unchanged (strict `/agent` extraction + exact output prefix match) and lock it with regression tests.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> Pass
- Baseline: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> Pass
- Baseline: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> Pass
- Final: `GOCACHE=/tmp/go-build go test ./...` -> Pass
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> Pass
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> Pass

## Residual Risks
- Fixtures cover current parser policy but do not yet include end-to-end issue-triage adversarial scenarios (planned in issue-first phase).

## Planning Refinements
- Updated `Task 5.3` scope to explicitly preserve dual-channel (`SIMUG:` / `SIMUG_MANAGER:`) routing rules in issue-triage prompt/protocol work.
