# Task 6.5: Failure-injection integration tests

## Objective
- Expand deterministic integration coverage for failure and recovery paths across protocol, command, restart, and issue-finalization behavior.

## Scope
- In: targeted integration tests, docs/changelog/planning/history updates.
- Out: real Codex runtime canaries (Task 6.5a+).

## Key Decisions
- Treated existing protocol-matrix and restart-recovery transition suites as core failure-injection coverage and added missing command-failure/finalization-failure integration cases.
- Added explicit no-PR fetch failure and merge close failure tests to cover real `git`/`gh` failure classes in orchestration paths.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Focused: `GOCACHE=/tmp/go-build go test ./internal/app -run 'TestRun(NoOpenPRFailsOnFetchOriginError|MergeFinalizationFailsWhenCloseIssueFails)$'` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Coverage: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Coverage summary: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> 68.9% total statements.

## Residual Risks
- Real Codex runtime canary behavior is not exercised by these fixture-driven tests; covered by 6.5a/6.5b.

## Planning Refinements
- Refined Task 6.5a to explicitly require malformed/partial output handling scenarios in real Codex canaries.
