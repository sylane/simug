# Task 6.9: Stop/restart chaos validation

## Objective
- Provide scripted stop/restart interruption validation with deterministic invariant checks and auditable output.

## Scope
- In: chaos validation script, runbook updates, script contract tests, docs/changelog/planning/history updates.
- Out: Phase 7 refactor/modularity work.

## Key Decisions
- Implemented chaos validation as a deterministic script with two interruption classes (`SIGTERM`, `SIGKILL`) followed by explicit restart runs.
- Added built-in invariant checks after each restart (clean tree + coherent state fields) and summary artifact generation.
- Linked chaos evidence as required input to the 6.8 go/no-go checklist.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Focused: `GOCACHE=/tmp/go-build go test ./cmd/simug` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Coverage: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Coverage summary: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> 68.9% total statements.

## Residual Risks
- Chaos script validates core invariants and restartability but does not exhaustively cover all live GitHub/Codex timing races.

## Planning Refinements
- None.
