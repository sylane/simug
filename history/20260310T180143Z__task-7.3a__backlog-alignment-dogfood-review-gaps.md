# Task 7.3a: Backlog alignment for dogfood review gaps

## Objective
- Add planning follow-up tasks for recently observed dogfood failures and missing review context so the next implementation work is queued explicitly.

## Scope
- In: `docs/PLANNING.md`, `CHANGELOG.md`, task history for this planning-only update
- Out: application code, protocol changes, GitHub API behavior changes

## Key Decisions
- Queue the review-context/reply fix, bootstrap single-commit guard, and post-merge local branch cleanup ahead of `Task 7.4` so the observed runtime gaps are addressed before loop modularization work.
- Treat the `../TEMP.md` bootstrap multi-commit report as a concrete input for a dedicated fail-closed task instead of burying it in a generic testing backlog item.
- Preserve the nuance from the code audit that no-PR intake already checks merged-branch ancestry, checks out `main`, and fast-forwards from `origin/main`; the missing behavior is deterministic local branch deletion plus explicit validation coverage.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> passed
- Baseline: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> passed
- Baseline: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> total coverage `70.9%`
- Final: `GOCACHE=/tmp/go-build go test ./...` -> passed
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> passed
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> total coverage `70.9%`
- Final: `GOCACHE=/tmp/go-build go build -o bin/simug ./cmd/simug` -> passed
- Final: `scripts/canary-real-codex-gate.sh` -> failed at existing Codex preflight blocker: unwritable `~/.codex` paths (`~/.codex/tmp/arg0`)

## Residual Risks
- This task only updates planning; the runtime bugs remain until `Task 7.3b` through `Task 7.3d` land.

## Planning Refinements
- Added `Task 7.3b`, `Task 7.3c`, and `Task 7.3d` ahead of `Task 7.4`.
