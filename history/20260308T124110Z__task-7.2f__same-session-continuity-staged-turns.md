# Task 7.2f: Same-session continuity for staged turns

## Objective
- Persist and reuse Codex session identity across staged `intent -> execute -> repair` turns with restart-safe state semantics.

## Scope
- In: state schema/session field, staged-turn command selection/resume behavior, continuity checks, tests, docs/changelog updates.
- Out: optional bootstrap context abstraction (`7.3`).

## Key Decisions
- Persisted `bootstrap_session_id` in state and tied its lifecycle to bootstrap context (`bootstrap_intent` / mode transitions).
- Captured staged session id from Codex artifact references in intent turn output, then reused it for execution/repair turns via `codex exec resume <id> -` when available.
- Added continuity mismatch guard: if a resumed execution turn emits a different observed session id, bootstrap execution fails closed.
- Kept session continuity optional when no session id can be observed (fixture commands and non-codex integrations).

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> total `70.1%`.
- Final: `scripts/canary-real-codex-gate.sh` -> fail (`codex preflight failed: runtime paths appear unwritable` in this environment).

## Residual Risks
- Optional bootstrap context abstraction (repo-specific docs/layout assumptions) remains pending; tracked by Task `7.3`.
- Real-Codex gate in this environment is still blocked by local runtime path permissions.

## Planning Refinements
- None.
