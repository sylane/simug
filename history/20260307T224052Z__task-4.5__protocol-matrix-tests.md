# Task 4.5: Prompt-driven simulated Codex protocol matrix tests

## Objective
- Add deterministic matrix tests for mixed stdout protocol behaviors across valid and invalid cases.

## Scope
- In: orchestrator/agent integration tests with realistic stdout fixtures.
- Out: runtime protocol redesign.

## Key Decisions
- Implement protocol matrix coverage at the `runAgentWithValidation` boundary to exercise both agent parsing and orchestrator validation behavior together.
- Use mixed-stdout fixtures that include narrative noise around protocol lines to model realistic Codex output.
- Keep matrix deterministic by pinning `MaxRepairAttempts=0` for these regression tests.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> Pass
- Baseline: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> Pass
- Baseline: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> Pass
- Final: `GOCACHE=/tmp/go-build go test ./...` -> Pass
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> Pass
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> Pass

## Residual Risks
- Matrix currently focuses on one-attempt mode; multi-attempt repair-loop interplay remains for prompt-tuning harness work.

## Planning Refinements
- Updated `Task 4.6` scope to explicitly reuse and extend the 4.5 protocol matrix corpus.
