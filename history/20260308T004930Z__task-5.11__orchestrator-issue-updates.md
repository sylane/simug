# Task 5.11: Orchestrator-owned issue updates during development

## Objective
- Convert tracked issue linkage intents into orchestrator-owned issue comments with deterministic dedupe markers.

## Scope
- In: issue comment posting flow, same-user scope guard, idempotent marker handling, state updates/tests, design/changelog updates.
- Out: merge-time issue closure/finalization.

## Key Decisions
- Converted tracked `issue_links` entries into orchestrator-owned issue comments through a dedicated processing pass after action application in managed/bootstrap modes.
- Enforced same-user scope first by resolving issue author before posting; out-of-scope issues are skipped without mutation.
- Used deterministic marker metadata (`simug:issue-update:v1`) and marker lookup to suppress duplicate comments and mark links as posted idempotently.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Focused: `GOCACHE=/tmp/go-build go test ./internal/github ./internal/app` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Coverage: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` + `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> total `67.1%`.

## Residual Risks
- Merge-time finalization/closure of fixed issues remains pending for task 5.12.

## Planning Refinements
- None.
