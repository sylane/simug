# Task 7.2c: Protocol parser hardening against prompt/template echoes

## Objective
- Ensure echoed prompt/template `SIMUG:` examples cannot be mistaken for canonical agent protocol output.

## Scope
- In: protocol ingestion/parser hardening, deterministic tests for echo contamination, docs/changelog updates.
- Out: post-execution report gate (`7.2d`) and attempt forensic enrichment (`7.2e`).

## Key Decisions
- Implemented sequence-level filtering in agent runner:
  - split protocol actions into terminal-terminated sequences,
  - remove template-example-only sequences when a distinct non-template sequence exists,
  - keep existing duplicate-sequence collapse for identical transcript repeats.
- Avoided line-level filtering to prevent dropping legitimate single `idle` responses that match template examples.
- Added parser tests for template-sequence filtering and runner behavior with mixed template + real protocol output.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> total `68.4%`.
- Final: `scripts/canary-real-codex-gate.sh` -> fail (`codex preflight failed: runtime paths appear unwritable` in this environment).

## Residual Risks
- Post-execution report payload gate and commit-proof validation remain pending; tracked by Task `7.2d`.
- Same-session continuity across staged intent/execute/repair turns remains pending; tracked by Task `7.2f`.
- Real-Codex gate remains blocked in this environment until Codex runtime path permissions are corrected.

## Planning Refinements
- None.
