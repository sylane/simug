# Task 6.10e: Runbook docs relocation to `docs/runbooks/`

## Objective
- Separate operator runbooks from top-level design/planning/workflow docs by relocating runbooks under `docs/runbooks/`.

## Scope
- In: file relocation and internal reference updates in docs/readme/agents/workflow.
- Out: behavior/runtime code changes.

## Key Decisions
- Keep operational runbooks as first-class docs, but isolate them under `docs/runbooks/` to avoid mixing operator procedures with core design/planning/workflow documents.
- Update all path references in README/WORKFLOW/AGENTS and cross-links inside runbooks in the same commit to avoid broken navigation.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `rg -n "docs/(REAL_CODEX_GATE|SANDBOX_DRY_RUN|SELF_HOST_CANARY|SELF_HOST_GO_NO_GO|CHAOS_STOP_RESTART)\\.md" README.md AGENTS.md docs scripts internal -S` -> no matches (all references relocated).

## Residual Risks
- None.

## Planning Refinements
- Added task `6.10e` for this relocation.
- None.
