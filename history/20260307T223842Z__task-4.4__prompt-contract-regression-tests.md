# Task 4.4: Prompt contract regression tests

## Objective
- Add tests that lock prompt contract requirements for managed, bootstrap, and repair prompts.

## Scope
- In: tests around prompt-builder required rules/examples.
- Out: prompt semantic redesign.

## Key Decisions
- Add dedicated unit tests that validate stable prompt-contract substrings for managed, bootstrap, and repair prompt builders.
- Keep assertions focused on high-signal requirements only (`SIMUG:` protocol examples, terminal-action constraint, no-push/no-direct-PR constraints) to avoid brittle over-testing.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> Pass
- Baseline: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> Pass
- Baseline: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> Pass
- Final: `GOCACHE=/tmp/go-build go test ./...` -> Pass
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> Pass
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> Pass

## Residual Risks
- Prompt tests currently enforce required textual contract fragments; they do not validate full semantic adequacy of generated prompts.

## Planning Refinements
- Updated `Task 4.5` scope to explicitly preserve 4.4 prompt-contract requirements while testing protocol-matrix fixtures.
