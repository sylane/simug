# Task 5.8: Issue-first integration and failure tests

## Objective
- Expand deterministic automated coverage for issue-first behavior across triage, planning insertion, issue comments, backlink idempotency, and no-actionable-issue fallback paths.

## Scope
- In: mocked integration and unit failure-path tests for issue-first pipeline contracts.
- Out: live GitHub end-to-end validation.

## Key Decisions
- Coverage focus: add deterministic failure tests where issue triage succeeds but planning insertion fails (`needs_task=true` with missing planning file), ensuring fail-closed behavior.
- Abuse-resistance coverage: add backlink-marker spoofing test to verify duplicate suppression only trusts authenticated-author comments.
- Pipeline coherence: preserve and extend integration coverage for pending-task bootstrap targeting and fallback/no-actionable-issue behavior.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Focused: `GOCACHE=/tmp/go-build go test ./internal/app` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Coverage: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out && GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> pass (`total: 66.0%`).

## Residual Risks
- Mocked command-runner integration tests still cannot model sequential command outputs for the same command key without custom scaffolding; live or richer harness validation remains valuable for PR-creation edge timing.

## Planning Refinements
- None.
