# Task 7.4a: Backlog alignment for protocol reliability and transcript logging

## Objective
- Queue the follow-up work needed to make coordinator protocol handling more reliable while keeping verbose progress useful and preserving full interaction forensics.

## Scope
- In: `docs/PLANNING.md`, `CHANGELOG.md`, task history for this planning-only update
- Out: protocol parser changes, runtime logging changes, UI behavior changes

## Key Decisions
- Queue protocol-envelope hardening ahead of generic Phase 7 cleanup so the next self-host work attacks the false multi-terminal class directly.
- Separate live operator progress from full transcript retention by making them distinct backlog items: one for concise verbose UX, one for durable complete logs and forensic fidelity.
- Treat execution-turn/gate decoupling as a dedicated task because environment-sensitive gate retries currently lengthen write-enabled turns and increase protocol cardinality risk.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> passed
- Final: `GOCACHE=/tmp/go-build go test ./...` -> passed
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> passed
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> total coverage `72.8%`
- Final: `GOCACHE=/tmp/go-build go build -o bin/simug ./cmd/simug` -> passed
- Final: `scripts/canary-real-codex-gate.sh` -> failed at existing Codex preflight blocker: unwritable `~/.codex` paths (`~/.codex/tmp/arg0`)

## Residual Risks
- This task only updates planning; the protocol and transcript issues remain until the follow-up implementation tasks land.

## Planning Refinements
- Added `Task 7.4b` through `Task 7.4e` immediately after `Task 7.4`.
