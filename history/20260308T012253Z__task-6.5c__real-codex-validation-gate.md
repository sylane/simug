# Task 6.5c: Real Codex validation gate integration

## Objective
- Integrate real Codex canaries into a repeatable operator/release gate with clear prerequisites and artifact retention.

## Scope
- In: gate script, gate runbook docs, script contract tests, changelog/planning/history updates.
- Out: live GitHub sandbox execution (Task 6.6).

## Key Decisions
- Implemented a single gate script that composes protocol + recovery canaries and emits one `summary.json` for auditable pass/fail evidence.
- Added retention cleanup policy (`--retain-days`) directly to gate runner to bound artifact sprawl while preserving recent evidence.
- Added dedicated gate runbook (`docs/REAL_CODEX_GATE.md`) with prerequisites, expected runtime/cost envelope, output structure, and failure procedure.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Focused: `GOCACHE=/tmp/go-build go test ./cmd/simug` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Coverage: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Coverage summary: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> 68.9% total statements.

## Residual Risks
- Gate execution still depends on live Codex runtime/session quality; this commit adds deterministic runner mechanics and runbook, not guaranteed external service stability.

## Planning Refinements
- Refined Task 6.6 to require 6.5c gate artifacts as prerequisite evidence before live GitHub sandbox dry-run.
