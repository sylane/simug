# Task 6.10c: Environment-configured Codex compatibility + passing real-Codex gate

## Objective
- Make real Codex integration work out of the box with environment-configured Codex and eliminate protocol false failures observed in real canary runs.

## Scope
- In: real-output ingestion/parsing robustness, canary validation, and related docs/planning/changelog updates.
- Out: alternate runtime backends (`app-server`/`--json` adapters) beyond what is needed to pass current real-Codex gate.

## Key Decisions
- Parse protocol output even when Codex command exits non-zero, and accept the run when protocol validation succeeds (`exactly one` terminal action).
- Collapse only identical repeated full protocol sequences (real transcript echoes) and keep distinct multiple terminal sequences as hard failures.
- Keep recovery canary malformed branch non-protocol (`MALFORMED:`) to force deterministic repair-path exercise without introducing parse ambiguity.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> pass (total: 68.6%).
- Final: `GOCACHE=/tmp/go-build go build -o bin/simug ./cmd/simug` -> pass.
- Final: `scripts/canary-real-codex-gate.sh --out /tmp/simug-gate-610c-final` -> pass in environment-configured Codex runtime (`codex exec`), gate summary at `/tmp/simug-gate-610c-final/gate-20260308T103205Z/summary.json`.

## Residual Risks
- Codex transcript formatting may evolve again; future structured-event ingestion tasks (Phase 9) are still needed to reduce reliance on text transcript normalization.

## Planning Refinements
- None.
