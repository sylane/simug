# Task 7.3b: Inline review context and review-reply correctness

## Objective
- Propagate inline review location context into managed-PR prompts and fix orchestrator review replies to use the correct GitHub endpoint shape.

## Scope
- In: review comment polling/modeling, managed-PR prompt rendering, GitHub review-reply call path, regression tests
- Out: bootstrap validation changes, merged-branch cleanup

## Key Decisions
- Preserve review location data as structured event metadata instead of concatenating it into the body, so prompt rendering can keep file/line/hunk fields explicit and testable.
- Fix the reply path in `internal/github` directly and cover it there, while separately covering prompt rendering in `internal/app` to keep the regressions narrow and deterministic.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> passed
- Final: `GOCACHE=/tmp/go-build go test ./internal/github ./internal/app` -> passed
- Final: `GOCACHE=/tmp/go-build go test ./...` -> passed
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> passed
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> total coverage `71.5%`
- Final: `GOCACHE=/tmp/go-build go build -o bin/simug ./cmd/simug` -> passed
- Final: `scripts/canary-real-codex-gate.sh` -> failed at existing Codex preflight blocker: unwritable `~/.codex` paths (`~/.codex/tmp/arg0`)

## Residual Risks
- GitHub only accepts review replies on top-level review comments; this task fixes the endpoint shape but does not add extra preflight classification beyond existing API failure handling.

## Planning Refinements
- None.
