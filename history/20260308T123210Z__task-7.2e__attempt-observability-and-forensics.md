# Task 7.2e: Attempt-level observability and forensic artifacts

## Objective
- Improve per-attempt artifacts and failure diagnostics with protocol excerpts and terminal-detection context.

## Scope
- In: attempt archive metadata enrichment, explain-last-failure signal improvements, tests, docs/changelog updates.
- Out: same-session continuity (`7.2f`).

## Key Decisions
- Extended per-attempt archive metadata to include:
  - protocol action counts/types and compact action excerpts,
  - terminal-action diagnostics and parser hints,
  - detected rollout/session references from raw agent output.
- Added diagnostics builder paths for both parsed-success and raw-error attempt outcomes.
- Updated `explain-last-failure` to surface new forensic fields directly in operator output.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> total `70.2%`.
- Final: `scripts/canary-real-codex-gate.sh` -> fail (`codex preflight failed: runtime paths appear unwritable` in this environment).

## Residual Risks
- Same-session continuity across staged intent/execute/repair turns remains pending; tracked by Task `7.2f`.
- Real-Codex gate in this environment is still blocked by local runtime path permissions.

## Planning Refinements
- None.
