# Task 6.10b: Codex runtime preflight diagnostics

## Objective
- Surface Codex runtime blockers early with actionable diagnostics (missing command, auth failure, unwritable Codex paths) while keeping Codex setup ownership outside simug.

## Scope
- In: startup/canary preflight checks and runtime failure classification for Codex-related errors; tests/docs/changelog updates.
- Out: automatic repair of Codex installation/auth/path permissions.

## Key Decisions
- Add a Codex-only startup preflight in simug (`codex ... --help`) so known runtime blockers are detected before orchestration starts.
- Reuse a shared runtime hint classifier in `internal/agent` for both preflight and command-failure diagnostics to avoid drift between canary and runtime behavior.
- Keep preflight scoped to Codex commands only; non-Codex `SIMUG_AGENT_CMD` values remain untouched.

## Validation
- Baseline: `GOCACHE=/tmp/go-build go test ./...` -> pass (from prior task before edits).
- Final: `gofmt -w internal/agent/diagnostics.go internal/agent/diagnostics_test.go internal/agent/runner.go internal/app/agent_preflight.go internal/app/agent_preflight_test.go internal/app/run.go internal/app/run_integration_test.go` -> pass.
- Final: `GOCACHE=/tmp/go-build go test ./internal/agent ./internal/app ./cmd/simug` -> pass.
- Final: `GOCACHE=/tmp/go-build GOEXPERIMENT=nocoverageredesign go test ./... -coverprofile=coverage.out` -> pass.
- Final: `GOCACHE=/tmp/go-build go tool cover -func=coverage.out` -> total `68.7%`.
- Final: `GOCACHE=/tmp/go-build go test ./...` -> pass.
- Final: `scripts/canary-real-codex-protocol.sh --out /tmp/simug-canary-check` -> expected fail-fast with actionable preflight diagnostic (`runtime paths appear unwritable`) and exit code `2`.

## Residual Risks
- Preflight help probes cannot guarantee full runtime success (for example model/provider outages after startup); task `6.10c` remains for broader canary reliability runbook hardening.

## Planning Refinements
- None.
